We will rewrite BulkImport.tsx to:
1. Actually show row-level validation errors (missing required fields, bad dates).
2. Allow ignoring invalid rows and importing only the valid ones.
3. Show progress UI.
